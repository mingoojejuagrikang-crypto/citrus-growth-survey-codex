# PM Todo

## Current objective
- Build the first runnable MVP slice of the citrus growth survey PWA.

## Next actions
- PM: add explicit sync queue, sync status, and retry handling beyond local-only storage
- PM: refine validation ranges and previous-value anomaly detection using real field rules
- PM: tighten workbook export/import compatibility for the two current sheets
- PM: improve structured STT parsing for more field aliases and safer numeric normalization on mobile browsers
- QA: run mobile browser checks for installability, refresh recovery, and same-subject update flow
